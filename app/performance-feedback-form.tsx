"use client";
import {FormEvent,useState} from "react";
export default function PerformanceFeedbackForm({assignmentId,type,close,done}:{assignmentId:number;type:string;close:()=>void;done:(message:string)=>void}){const[saving,setSaving]=useState(false),[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);const f=new FormData(e.currentTarget),status=String(f.get("submitType")||"Taslak"),payload={action:"feedback",assignmentId,reviewerType:type,score:Number(f.get("score")||0),strengths:String(f.get("strengths")||""),developmentAreas:String(f.get("developmentAreas")||""),comment:String(f.get("comment")||""),anonymous:type==="360°",status},r=await fetch("/api/performance",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)}),j=await r.json();setSaving(false);if(!r.ok){setError(j.error||"Değerlendirme kaydedilemedi");return}done(status==="Taslak"?"Değerlendirme taslak olarak kaydedildi":"Değerlendirme tamamlandı")}return <div className="modal-backdrop feedback-backdrop"><form className="record-modal feedback-form" onSubmit={submit}><header><div><span>PERFORMANS DEĞERLENDİRMESİ</span><h2>{type}</h2><p>Puan, güçlü yön, gelişim alanı ve dönem yorumunu kaydedin.</p></div><button type="button" onClick={close}>×</button></header><div className="feedback-score"><label><span>Genel Puan · 0–100</span><input name="score" type="number" min="0" max="100" required defaultValue="80"/></label><p>1–59 gelişim gerekli · 60–79 beklentiyi karşılıyor · 80–89 beklentinin üzerinde · 90–100 üstün</p></div><div className="form-grid"><label className="field wide"><span>Güçlü Yönler</span><textarea name="strengths" required placeholder="Çalışanın güçlü olduğu davranış ve sonuçları yazın."/></label><label className="field wide"><span>Gelişim Alanları</span><textarea name="developmentAreas" required placeholder="Geliştirilmesi gereken ölçülebilir alanları yazın."/></label><label className="field wide"><span>Dönem Başarıları ve Genel Yorum</span><textarea name="comment" required placeholder="Hedef, KPI, proje çıktıları ve gelecek dönem önerileri."/></label></div>{type==="360°"&&<p className="anonymous-note">360° sonucu çalışana anonim ortalama olarak gösterilir; değerlendirici bilgisi yetkiyle korunur.</p>}{error&&<div className="form-error">{error}</div>}<footer><button type="button" onClick={close}>İptal</button><button name="submitType" value="Taslak" disabled={saving}>Taslak Kaydet</button><button className="primary" name="submitType" value="Tamamlandı" disabled={saving}>{saving?"Kaydediliyor…":"Değerlendirmeyi Tamamla"}</button></footer></form></div>}

"use client";import {useEffect} from "react";
const labels:Record<number,string>={0:"Değerlendirilmedi",20:"Çok Düşük",40:"Gelişim Gerekli",60:"Beklentiyi Karşılıyor",80:"Başarılı",100:"Üstün Performans"};
export default function PerformanceScoreSelects(){useEffect(()=>{function enhance(){document.querySelectorAll<HTMLLabelElement>(".score-field").forEach((field,index)=>{const input=field.querySelector<HTMLInputElement>('input[type="number"]');if(!input||input.dataset.selectReady)return;input.dataset.selectReady="1";input.style.display="none";const title=field.querySelector("span"),weight=[30,40,30][index%3];if(title&&!title.textContent?.includes("Ağırlık"))title.textContent=(title.textContent||"")+" · Ağırlık %"+weight+" (otomatik)";const select=document.createElement("select");select.setAttribute("aria-label",title?.textContent||"Puan seçimi");for(let n=0;n<=100;n+=5){const option=document.createElement("option");option.value=String(n);const nearest=n===0?0:n<=30?20:n<=50?40:n<=70?60:n<=90?80:100;option.textContent=n+" puan · "+labels[nearest];select.append(option)}select.value=input.value||"0";select.addEventListener("change",()=>{const setter=Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,"value")?.set;setter?.call(input,select.value);input.dispatchEvent(new Event("input",{bubbles:true}));input.dispatchEvent(new Event("change",{bubbles:true}))});input.addEventListener("input",()=>{select.value=input.value||"0"});field.append(select)})}enhance();const observer=new MutationObserver(enhance);observer.observe(document.body,{childList:true,subtree:true});return()=>observer.disconnect()},[]);return null}

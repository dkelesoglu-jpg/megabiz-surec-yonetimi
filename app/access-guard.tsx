"use client";import {useEffect} from "react";
export default function AccessGuard(){useEffect(()=>{let alive=true;fetch("/api/access").then(r=>r.json()).then(j=>{if(!alive||!j.permissions)return;const map=new Map<string,{canView:boolean;canEdit:boolean}>(j.permissions.map((p:{module:string;canView:boolean;canEdit:boolean})=>[p.module,p]));document.querySelectorAll<HTMLButtonElement>(".nav-item").forEach(button=>{const name=(button.textContent||"").trim().replace(/^[⌂◇◎↗✓≡▤□·]\s*/,"");const p=map.get(name);if(p&&!p.canView)button.style.display="none"});const active=document.querySelector(".topbar h1")?.textContent?.trim()||"Genel Bakış",permission=map.get(active);if(permission&&!permission.canEdit)document.querySelectorAll<HTMLButtonElement>(".primary,.record-actions button,.all-actions").forEach(b=>{b.style.display="none"});document.body.dataset.userRole=j.role}) .catch(()=>{});return()=>{alive=false}},[]);return null}
